using TECH.Procedural;
using UnityEditor;

/*
[CustomEditor(typeof(GenerationManager))]
public class MyEditorClass : Editor
{
	public override void OnInspectorGUI()
	{
		// If we call base the default inspector will get drawn too.
		// Remove this line if you don't want that to happen.
		base.OnInspectorGUI();

		GenerationManager myBehaviour = target as GenerationManager;

		target. = EditorGUILayout.Toggle("Enable seed", target.myBool);

		if (target.myBool)
		{
			target.someFloat = EditorGUILayout.FloatField ("Some Float:", target.someFloat);
		}
	}
}
**/